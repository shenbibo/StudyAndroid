<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.study.sky.study">

    <application
        android:name=".application.StudyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/StudyAppTheme">
        <activity android:name=".modules.main.view.PreActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".modules.main.view.MainActivity" />

        <!--主界面-->
        <activity android:name=".modules.main.view.MainActivity2"/>
    </application>

</manifest>